# Local final-year-project reference

The previous thesis files `fyp_main.tex` and `refs.bib`, when present, are
local idea/history references. They are intentionally Git-ignored, are not
imported by `../conference_101719.tex`, and are not used by the paper asset
generators or the INFOCOM evidence chain.

This README remains tracked so the separation is explicit. The current
manuscript has its own inline IEEE `thebibliography` and does not depend on
the local archived files.
